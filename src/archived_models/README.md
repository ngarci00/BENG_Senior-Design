# Archived Models

This directory holds model families that are no longer part of the active hybrid pipeline.

- `run_2DCNN/`: the older standalone 2D CNN pipeline
- `run_3DCNN/`: the older standalone 3D CNN pipeline

The active SVM + frame-embedding pipeline now lives in `src/run_HYBRID/`.
