"""This code serves as a rough template for a 3D CNN model using PyTorch for video classification 
LabelMe gives us data in JSON files that we need to convert to a usable format for training the model.
- Each JSON file contains annotations for a video, which includes the path and boxes for each frame.
- We will extgract the relevant information from these JSON files & use it to create the dataset. """

from eti.run import main #Imports the main function from the eit.run module
if __name__ == "__main__":
    main() #Calls the main function when the script is executed
